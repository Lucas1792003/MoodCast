{
  "name": "moodcast-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "web": "npm --prefix apps/web run dev",
    "mobile": "npm --prefix apps/mobile run start",
    "build:web": "npm --prefix apps/web run build"
  }
}
